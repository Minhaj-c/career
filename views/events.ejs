<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Upcoming Events</title>
    <style>
        .event-card {
            border: 1px solid #ccc;
            border-radius: 8px;
            padding: 15px;
            margin-bottom: 10px;
        }
        .event-card h3 {
            margin: 0;
        }
        .event-card p {
            margin: 5px 0;
        }
        .event-card a {
            color: #4CAF50;
            text-decoration: none;
        }
        .event-card a:hover {
            text-decoration: underline;
        }
    </style>
</head>
<body>
    <header>
        <h1>Upcoming Events</h1>
        <p>Welcome, <%= username %>! Here are the latest events.</p>
    </header>

    <main>
        <% if (events.length > 0) { %>
            <% events.forEach(event => { %>
                <div class="event-card">
                    <h3><%= event.title %></h3>
                    <p><%= event.description %></p>
                    <p><strong>Date:</strong> <%= event.date.toLocaleDateString() %></p>
                    <p><strong>Location:</strong> <%= event.location %></p>
                    <% if (event.url) { %>
                        <a href="<%= event.url %>" target="_blank">Learn More</a>
                    <% } %>
                </div>
            <% }) %>
        <% } else { %>
            <p>No upcoming events found.</p>
        <% } %>
    </main>

    <footer>
        <button onclick="window.location.href='/home/<%= userId %>'">Go Back to Home</button>
    </footer>
</body>
</html>