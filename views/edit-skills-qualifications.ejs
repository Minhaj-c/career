<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Skills & Qualifications</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" />
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/animate.css/4.1.1/animate.min.css" />
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap');
      
        :root {
            --primary-color: #2a3990;
            --secondary-color: #4e54c8;
            --accent-color: #ff6b6b;
            --background-color: #f8f9ff;
            --card-color: #ffffff;
            --text-color: #333344;
            --light-text: #7b7b8f;
            --shadow: 0 8px 30px rgba(0, 0, 0, 0.08);
            --hover-shadow: 0 15px 35px rgba(0, 0, 0, 0.12);
            --transition: all 0.3s cubic-bezier(0.25, 0.8, 0.25, 1);
        }

        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
        }

        body {
            font-family: 'Poppins', sans-serif;
            margin: 0;
            padding: 0;
            background-color: var(--background-color);
            color: var(--text-color);
            overflow-x: hidden;
        }

        /* Header styles */
        header {
            background-color: var(--card-color);
            padding: 15px 30px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            position: sticky;
            top: 0;
            box-shadow: var(--shadow);
            z-index: 1000;
            transition: var(--transition);
        }

        header:hover {
            box-shadow: var(--hover-shadow);
        }

        header h1 {
            color: var(--primary-color);
            font-size: 24px;
            font-weight: 700;
            position: relative;
            padding-bottom: 5px;
            margin: 0;
        }

        header h1::after {
            content: '';
            position: absolute;
            bottom: 0;
            left: 0;
            width: 50px;
            height: 3px;
            background: linear-gradient(135deg, var(--primary-color), var(--secondary-color));
            border-radius: 2px;
        }

        /* Main content styles */
        main {
            max-width: 800px;
            margin: 40px auto;
            padding: 40px;
            background-color: var(--card-color);
            border-radius: 20px;
            box-shadow: var(--shadow);
            transition: var(--transition);
            animation: fadeInUp 0.5s ease-out;
        }

        main:hover {
            box-shadow: var(--hover-shadow);
        }

        @keyframes fadeInUp {
            from {
                opacity: 0;
                transform: translateY(20px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        /* Form styles */
        form {
            display: flex;
            flex-direction: column;
            gap: 30px;
        }

        .input-group {
            display: flex;
            flex-direction: column;
            gap: 10px;
        }

        label {
            font-weight: 600;
            color: var(--primary-color);
            font-size: 16px;
            display: flex;
            align-items: center;
            gap: 8px;
        }

        label i {
            color: var(--secondary-color);
        }

        .multiselect {
            width: 100%;
            position: relative;
        }

        .multiselect .selectBox {
            position: relative;
            background-color: white;
            border: 2px solid rgba(78, 84, 200, 0.2);
            border-radius: 12px;
            transition: var(--transition);
            cursor: pointer;
        }

        .multiselect .selectBox:hover {
            border-color: var(--primary-color);
        }

        .multiselect .selectBox select {
            width: 100%;
            height: 45px;
            font-size: 14px;
            padding: 0 15px;
            cursor: pointer;
            font-family: 'Poppins', sans-serif;
            background-color: transparent;
            border: none;
            outline: none;
            appearance: none;
            color: var(--text-color);
        }

        .multiselect .selectBox::after {
            content: '\f0d7';
            font-family: 'Font Awesome 5 Free';
            font-weight: 900;
            position: absolute;
            right: 15px;
            top: 50%;
            transform: translateY(-50%);
            color: var(--primary-color);
            pointer-events: none;
        }

        .multiselect .overSelect {
            position: absolute;
            left: 0;
            right: 0;
            top: 0;
            bottom: 0;
        }

        .multiselect .checkboxes {
            display: none;
            border: 1px solid rgba(78, 84, 200, 0.2);
            border-top: none;
            border-radius: 0 0 12px 12px;
            position: absolute;
            background-color: white;
            width: 100%;
            z-index: 10;
            max-height: 200px;
            overflow-y: auto;
            margin-top: 5px;
            box-shadow: 0 10px 25px rgba(0, 0, 0, 0.1);
            animation: fadeIn 0.2s ease-out;
        }

        @keyframes fadeIn {
            from {
                opacity: 0;
                transform: translateY(-10px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .multiselect .checkboxes label {
            display: flex;
            align-items: center;
            padding: 12px 15px;
            font-weight: 400;
            color: var(--text-color);
            border-bottom: 1px solid rgba(78, 84, 200, 0.1);
            cursor: pointer;
            transition: var(--transition);
        }

        .multiselect .checkboxes label:last-child {
            border-bottom: none;
        }

        .multiselect .checkboxes label:hover {
            background-color: rgba(78, 84, 200, 0.05);
        }

        .multiselect .checkboxes input[type="checkbox"] {
            margin-right: 10px;
            width: 18px;
            height: 18px;
            accent-color: var(--primary-color);
            cursor: pointer;
        }

        .btn {
            margin-top: 20px;
            padding: 14px 30px;
            background: linear-gradient(135deg, var(--primary-color), var(--secondary-color));
            color: white;
            border: none;
            border-radius: 30px;
            font-weight: 600;
            font-size: 16px;
            cursor: pointer;
            transition: var(--transition);
            align-self: flex-start;
            box-shadow: 0 4px 15px rgba(42, 57, 144, 0.2);
            position: relative;
            overflow: hidden;
        }

        .btn::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.2), transparent);
            transition: all 0.6s;
        }

        .btn:hover {
            transform: translateY(-3px);
            box-shadow: 0 7px 20px rgba(42, 57, 144, 0.3);
        }

        .btn:hover::before {
            left: 100%;
        }

        /* Responsive styles */
        @media (max-width: 768px) {
            header {
                padding: 15px 20px;
            }
            
            main {
                padding: 30px 20px;
                margin: 30px 20px;
            }
            
            header h1 {
                font-size: 20px;
            }
        }

        @media (max-width: 480px) {
            header {
                padding: 12px 15px;
            }
            
            main {
                padding: 20px 15px;
                margin: 20px 15px;
            }
            
            header h1 {
                font-size: 18px;
            }
            
            .btn {
                width: 100%;
                text-align: center;
            }
        }
    </style>
</head>
<body>
    <header>
        <h1>Edit <span style="color: var(--accent-color);"><%= user.username %></span>'s Skills & Qualifications</h1>
    </header>

    <main>
        <form action="/update-skills-qualifications" method="POST">
            <input type="hidden" name="userId" value="<%= user._id %>">

            <div class="input-group">
                <label for="skills"><i class="fas fa-code"></i> Skills</label>
                <div class="multiselect">
                    <div class="selectBox" onclick="showCheckboxes('skills')">
                        <select>
                            <option>Select your technical skills</option>
                        </select>
                        <div class="overSelect"></div>
                    </div>
                    <div id="skills" class="checkboxes">
                        <label for="skill1"><input type="checkbox" id="skill1" name="skills" value="JavaScript" <%= user.skills.includes("JavaScript") ? "checked" : "" %>> JavaScript</label>
                        <label for="skill2"><input type="checkbox" id="skill2" name="skills" value="Python" <%= user.skills.includes("Python") ? "checked" : "" %>> Python</label>
                        <label for="skill3"><input type="checkbox" id="skill3" name="skills" value="Java" <%= user.skills.includes("Java") ? "checked" : "" %>> Java</label>
                        <label for="skill4"><input type="checkbox" id="skill4" name="skills" value="Nothing" <%= user.skills.includes("Nothing") ? "checked" : "" %>> Nothing</label>
                    </div>
                </div>
            </div>

            <div class="input-group">
                <label for="interests"><i class="fas fa-lightbulb"></i> Interests</label>
                <div class="multiselect">
                    <div class="selectBox" onclick="showCheckboxes('interests')">
                        <select>
                            <option>Select your professional interests</option>
                        </select>
                        <div class="overSelect"></div>
                    </div>
                    <div id="interests" class="checkboxes">
                        <label for="interest1"><input type="checkbox" id="interest1" name="interests" value="Web Development" <%= user.interests.includes("Web Development") ? "checked" : "" %>> Web Development</label>
                        <label for="interest2"><input type="checkbox" id="interest2" name="interests" value="Data Science" <%= user.interests.includes("Data Science") ? "checked" : "" %>> Data Science</label>
                        <label for="interest3"><input type="checkbox" id="interest3" name="interests" value="Machine Learning" <%= user.interests.includes("Machine Learning") ? "checked" : "" %>> Machine Learning</label>
                       
                        <label for="interest5"><input type="checkbox" id="interest5" name="interests" value="Nothing" <%= user.interests.includes("Nothing") ? "checked" : "" %>> Nothing</label>
                    </div>
                </div>
            </div>

            <div class="input-group">
                <label for="hobbies"><i class="fas fa-gamepad"></i> Hobbies</label>
                <div class="multiselect">
                    <div class="selectBox" onclick="showCheckboxes('hobbies')">
                        <select>
                            <option>Select your personal hobbies</option>
                        </select>
                        <div class="overSelect"></div>
                    </div>
                    <div id="hobbies" class="checkboxes">
                        <label for="hobby1"><input type="checkbox" id="hobby1" name="hobbies" value="Gaming" <%= user.hobbies.includes("Gaming") ? "checked" : "" %>> Gaming</label>
                        <label for="hobby2"><input type="checkbox" id="hobby2" name="hobbies" value="Reading" <%= user.hobbies.includes("Reading") ? "checked" : "" %>> Reading</label>
                        <label for="hobby3"><input type="checkbox" id="hobby3" name="hobbies" value="Traveling" <%= user.hobbies.includes("Traveling") ? "checked" : "" %>> Traveling</label>
                        <label for="hobby4"><input type="checkbox" id="hobby4" name="hobbies" value="Nothing" <%= user.hobbies.includes("Nothing") ? "checked" : "" %>> Nothing</label>
                    </div>
                </div>
            </div>

            <button type="submit" class="btn btn-primary">
                <i class="fas fa-save"></i> Save Changes
            </button>
        </form>
    </main>

    <script>
        function showCheckboxes(id) {
            var checkboxes = document.getElementById(id);
            var allCheckboxes = document.querySelectorAll('.checkboxes');
            
            // Close all other open checkbox lists
            allCheckboxes.forEach(function(item) {
                if (item.id !== id && item.style.display === "block") {
                    item.style.display = "none";
                }
            });
            
            // Toggle the selected one
            if (checkboxes.style.display === "block") {
                checkboxes.style.display = "none";
            } else {
                checkboxes.style.display = "block";
            }
        }
        
        // Close checkboxes when clicking outside
        document.addEventListener('click', function(event) {
            if (!event.target.closest('.multiselect')) {
                document.querySelectorAll('.checkboxes').forEach(function(el) {
                    el.style.display = "none";
                });
            }
        });
    </script>
</body>
</html>