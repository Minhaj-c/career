
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Career Guidance - Your Path to a Better Future</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" />
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/animate.css/4.1.1/animate.min.css" />
    <style>
      @import url('https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap');
      
      :root {
        --primary-color: #2a3990;
        --secondary-color: #4e54c8;
        --accent-color: #ff6b6b;
        --background-color: #f8f9ff;
        --card-color: #ffffff;
        --text-color: #333344;
        --light-text: #7b7b8f;
        --shadow: 0 8px 30px rgba(0, 0, 0, 0.08);
        --hover-shadow: 0 15px 35px rgba(0, 0, 0, 0.12);
        --transition: all 0.3s cubic-bezier(0.25, 0.8, 0.25, 1);
      }

      * {
        box-sizing: border-box;
        margin: 0;
        padding: 0;
      }

      body {
        font-family: 'Poppins', sans-serif;
        margin: 0;
        padding: 0;
        background-color: var(--background-color);
        color: var(--text-color);
        overflow-x: hidden;
      }

      /* Header styles */
      header {
        background-color: var(--card-color);
        padding: 15px 30px;
        display: flex;
        justify-content: space-between;
        align-items: center;
        position: sticky;
        top: 0;
        box-shadow: var(--shadow);
        z-index: 1000;
        transition: var(--transition);
      }

      header:hover {
        box-shadow: var(--hover-shadow);
      }

      .header-content {
        display: flex;
        align-items: center;
        gap: 20px;
      }

      .logo {
        font-weight: 700;
        font-size: 24px;
        color: var(--primary-color);
        text-decoration: none;
        display: flex;
        align-items: center;
        gap: 10px;
      }

      .logo-icon {
        background: linear-gradient(135deg, var(--primary-color), var(--secondary-color));
        color: white;
        width: 36px;
        height: 36px;
        border-radius: 8px;
        display: flex;
        align-items: center;
        justify-content: center;
      }

      .profile-pic-container {
        position: relative;
        display: inline-block;
      }

      .profile-pic {
        width: 45px;
        height: 45px;
        border-radius: 50%;
        cursor: pointer;
        border: 2px solid transparent;
        transition: var(--transition);
        object-fit: cover;
      }

      .profile-pic:hover {
        border-color: var(--accent-color);
        transform: scale(1.05);
      }

      .dropdown-content {
        display: none;
        position: absolute;
        right: 0;
        background-color: var(--card-color);
        min-width: 200px;
        box-shadow: var(--shadow);
        z-index: 1;
        border-radius: 12px;
        overflow: hidden;
        transform: translateY(10px);
        opacity: 0;
        transition: var(--transition);
      }

      .profile-pic-container:hover .dropdown-content {
        display: block;
        transform: translateY(0);
        opacity: 1;
        animation: fadeIn 0.3s ease-out forwards;
      }

      @keyframes fadeIn {
        from {
          opacity: 0;
          transform: translateY(10px);
        }
        to {
          opacity: 1;
          transform: translateY(0);
        }
      }

      .dropdown-content a {
        color: var(--text-color);
        padding: 12px 20px;
        text-decoration: none;
        display: flex;
        align-items: center;
        gap: 12px;
        transition: var(--transition);
      }

      .dropdown-content a:hover {
        background-color: rgba(78, 84, 200, 0.1);
        color: var(--primary-color);
      }

      .dropdown-content a i {
        color: var(--primary-color);
        width: 20px;
        text-align: center;
      }

      .welcome-text {
        display: flex;
        flex-direction: column;
      }

      .welcome-text h1 {
        font-size: 18px;
        margin: 0;
        color: var(--text-color);
        font-weight: 600;
      }

      .welcome-text h2 {
        font-size: 14px;
        font-weight: 400;
        margin: 0;
        color: var(--light-text);
      }

      .mode-toggle {
        display: flex;
        background-color: #f1f1fa;
        padding: 5px;
        border-radius: 30px;
      }

      .mode-button {
        padding: 8px 20px;
        border: none;
        border-radius: 25px;
        background-color: transparent;
        color: var(--light-text);
        cursor: pointer;
        transition: var(--transition);
        font-weight: 500;
      }

      .mode-button.active {
        background-color: var(--card-color);
        color: var(--primary-color);
        box-shadow: 0 3px 8px rgba(0, 0, 0, 0.05);
      }

      /* Main content styles */
      main {
        max-width: 1200px;
        margin: 0 auto;
        padding: 40px 20px;
      }

      .hero-section {
        text-align: center;
        margin-bottom: 60px;
        animation: fadeInUp 1s ease-out;
      }

      @keyframes fadeInUp {
        from {
          opacity: 0;
          transform: translateY(20px);
        }
        to {
          opacity: 1;
          transform: translateY(0);
        }
      }

      .hero-section h1 {
        font-size: 44px;
        background: linear-gradient(135deg, var(--primary-color), var(--secondary-color));
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        margin-bottom: 15px;
        position: relative;
        display: inline-block;
      }

      .hero-section h1::after {
        content: '';
        position: absolute;
        bottom: -10px;
        left: 50%;
        transform: translateX(-50%);
        width: 100px;
        height: 4px;
        background: linear-gradient(135deg, var(--primary-color), var(--secondary-color));
        border-radius: 2px;
      }

      .hero-section h2 {
        font-size: 20px;
        color: var(--light-text);
        font-weight: 400;
        margin-top: 20px;
        max-width: 600px;
        margin-left: auto;
        margin-right: auto;
      }

      /* Features grid */
      .features-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
        gap: 30px;
        margin-top: 50px;
      }

      .feature-card {
        background-color: var(--card-color);
        border-radius: 20px;
        overflow: hidden;
        box-shadow: var(--shadow);
        text-align: center;
        padding: 40px 25px;
        transition: var(--transition);
        position: relative;
        z-index: 1;
        height: 100%;
        display: flex;
        flex-direction: column;
      }

      .feature-card::before {
        content: '';
        position: absolute;
        top: 0;
        left: 0;
        right: 0;
        height: 0;
        background: linear-gradient(135deg, rgba(42, 57, 144, 0.03), rgba(78, 84, 200, 0.05));
        transition: var(--transition);
        z-index: -1;
      }

      .feature-card:hover {
        transform: translateY(-10px);
        box-shadow: var(--hover-shadow);
      }

      .feature-card:hover::before {
        height: 100%;
      }

      .feature-icon {
        width: 70px;
        height: 70px;
        background: linear-gradient(135deg, rgba(42, 57, 144, 0.1), rgba(78, 84, 200, 0.1));
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
        margin: 0 auto 25px;
        transition: var(--transition);
      }

      .feature-card:hover .feature-icon {
        transform: scale(1.1) rotate(5deg);
        background: linear-gradient(135deg, rgba(42, 57, 144, 0.15), rgba(78, 84, 200, 0.15));
      }

      .feature-icon i {
        font-size: 24px;
        color: var(--primary-color);
        transition: var(--transition);
      }

      .feature-card:hover .feature-icon i {
        transform: scale(1.1);
      }

      .feature-card h3 {
        color: var(--primary-color);
        margin-bottom: 15px;
        font-size: 20px;
        font-weight: 600;
        letter-spacing: 0.5px;
      }

      .feature-card p {
        color: var(--light-text);
        margin-bottom: 25px;
        flex-grow: 1;
      }

      .button {
        display: inline-block;
        padding: 12px 28px;
        background: linear-gradient(135deg, var(--primary-color), var(--secondary-color));
        color: white;
        border: none;
        border-radius: 30px;
        cursor: pointer;
        text-decoration: none;
        font-weight: 500;
        transition: var(--transition);
        position: relative;
        overflow: hidden;
        z-index: 1;
        box-shadow: 0 4px 15px rgba(42, 57, 144, 0.2);
      }

      .button::before {
        content: '';
        position: absolute;
        top: 0;
        left: -100%;
        width: 100%;
        height: 100%;
        background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.2), transparent);
        transition: all 0.6s;
        z-index: -1;
      }

      .button:hover {
        transform: translateY(-3px);
        box-shadow: 0 7px 20px rgba(42, 57, 144, 0.3);
      }

      .button:hover::before {
        left: 100%;
      }

      .button.outline {
        background: transparent;
        border: 2px solid var(--primary-color);
        color: var(--primary-color);
        box-shadow: none;
      }

      .button.outline:hover {
        background: rgba(42, 57, 144, 0.05);
        transform: translateY(-3px);
        box-shadow: 0 5px 15px rgba(42, 57, 144, 0.1);
      }

      /* Community section */
      .community-section {
        margin-top: 80px;
        background-color: var(--card-color);
        padding: 60px 40px;
        border-radius: 20px;
        text-align: center;
        box-shadow: var(--shadow);
        position: relative;
        overflow: hidden;
      }

      .community-section::before {
        content: '';
        position: absolute;
        top: 0;
        right: 0;
        width: 200px;
        height: 200px;
        background: linear-gradient(135deg, rgba(78, 84, 200, 0.1), rgba(255, 107, 107, 0.1));
        border-radius: 50%;
        transform: translate(30%, -30%);
        z-index: 0;
      }

      .community-section::after {
        content: '';
        position: absolute;
        bottom: 0;
        left: 0;
        width: 150px;
        height: 150px;
        background: linear-gradient(135deg, rgba(42, 57, 144, 0.1), rgba(78, 84, 200, 0.1));
        border-radius: 50%;
        transform: translate(-30%, 30%);
        z-index: 0;
      }

      .community-section h2 {
        color: var(--primary-color);
        margin-bottom: 20px;
        font-size: 28px;
        position: relative;
        z-index: 1;
      }

      .community-section p {
        color: var(--light-text);
        margin-bottom: 30px;
        max-width: 600px;
        margin-left: auto;
        margin-right: auto;
        position: relative;
        z-index: 1;
      }

      /* Resources section */
      .resources-section {
        margin-top: 80px;
        animation: fadeIn 1s ease-out;
      }

      .section-title {
        text-align: center;
        margin-bottom: 40px;
        color: var(--primary-color);
        font-size: 28px;
        position: relative;
        padding-bottom: 15px;
      }

      .section-title::after {
        content: '';
        position: absolute;
        bottom: 0;
        left: 50%;
        transform: translateX(-50%);
        width: 80px;
        height: 3px;
        background: linear-gradient(135deg, var(--primary-color), var(--secondary-color));
        border-radius: 2px;
      }

      .resource-item {
        background-color: var(--card-color);
        border-radius: 15px;
        padding: 25px;
        margin-bottom: 20px;
        box-shadow: var(--shadow);
        transition: var(--transition);
        transform: translateY(0);
      }

      .resource-item:hover {
        transform: translateY(-5px);
        box-shadow: var(--hover-shadow);
      }

      .resource-item h4 {
        color: var(--primary-color);
        margin-bottom: 10px;
        font-size: 18px;
      }

      .resource-item p {
        color: var(--light-text);
        margin-bottom: 15px;
      }

      .resource-link {
        color: var(--accent-color);
        text-decoration: none;
        font-weight: 500;
        display: inline-flex;
        align-items: center;
        gap: 5px;
        transition: var(--transition);
      }

      .resource-link:hover {
        color: var(--primary-color);
        gap: 8px;
      }

      .text-center {
        text-align: center;
      }

      /* Animations */
      .animate-card {
        animation: cardAppear 0.5s ease-out forwards;
        opacity: 0;
        transform: translateY(20px);
      }

      @keyframes cardAppear {
        to {
          opacity: 1;
          transform: translateY(0);
        }
      }

      /* Progress bar for skills */
      .progress-container {
        width: 100%;
        height: 8px;
        background-color: #f1f1fa;
        border-radius: 10px;
        margin: 10px 0 20px;
        overflow: hidden;
      }

      .progress-bar {
        height: 100%;
        background: linear-gradient(90deg, var(--primary-color), var(--secondary-color));
        border-radius: 10px;
        width: 0%;
        transition: width 1s ease-in-out;
      }

      /* Tooltip */
      .tooltip {
        position: relative;
        display: inline-block;
      }

      .tooltip .tooltip-text {
        visibility: hidden;
        width: 120px;
        background-color: var(--primary-color);
        color: #fff;
        text-align: center;
        border-radius: 6px;
        padding: 5px;
        position: absolute;
        z-index: 1;
        bottom: 125%;
        left: 50%;
        transform: translateX(-50%);
        opacity: 0;
        transition: opacity 0.3s;
      }

      .tooltip:hover .tooltip-text {
        visibility: visible;
        opacity: 1;
      }

      /* Notifications dot */
      .notification-dot {
        position: absolute;
        top: 0;
        right: 0;
        width: 10px;
        height: 10px;
        background-color: var(--accent-color);
        border-radius: 50%;
        box-shadow: 0 0 0 2px var(--card-color);
      }

      /* Footer styles */
      footer {
        background-color: var(--primary-color);
        color: white;
        padding: 50px 20px 30px;
        margin-top: 80px;
      }

      .footer-content {
        max-width: 1200px;
        margin: 0 auto;
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
        gap: 40px;
      }

      .footer-column h3 {
        font-size: 18px;
        margin-bottom: 20px;
        position: relative;
        padding-bottom: 10px;
      }

      .footer-column h3::after {
        content: '';
        position: absolute;
        bottom: 0;
        left: 0;
        width: 40px;
        height: 2px;
        background-color: var(--accent-color);
      }

      .footer-links {
        list-style: none;
        padding: 0;
      }

      .footer-links li {
        margin-bottom: 12px;
      }

      .footer-links a {
        color: rgba(255, 255, 255, 0.8);
        text-decoration: none;
        transition: var(--transition);
        display: flex;
        align-items: center;
        gap: 8px;
      }

      .footer-links a:hover {
        color: white;
        transform: translateX(5px);
      }

      .footer-links a i {
        font-size: 14px;
      }

      .social-links {
        display: flex;
        gap: 15px;
        margin-top: 20px;
      }

      .social-links a {
        display: flex;
        align-items: center;
        justify-content: center;
        width: 36px;
        height: 36px;
        background-color: rgba(255, 255, 255, 0.1);
        border-radius: 50%;
        color: white;
        transition: var(--transition);
      }

      .social-links a:hover {
        background-color: var(--accent-color);
        transform: translateY(-3px);
      }

      .footer-bottom {
        max-width: 1200px;
        margin: 40px auto 0;
        padding-top: 20px;
        border-top: 1px solid rgba(255, 255, 255, 0.1);
        text-align: center;
        color: rgba(255, 255, 255, 0.6);
        font-size: 14px;
      }

      /* Responsive design */
      @media (max-width: 1024px) {
        .features-grid {
          grid-template-columns: repeat(2, 1fr);
        }
      }

      @media (max-width: 768px) {
        .header-content {
          gap: 10px;
        }
        
        .welcome-text h1 {
          font-size: 16px;
        }
        
        .welcome-text h2 {
          font-size: 12px;
        }
        
        .mode-button {
          padding: 8px 15px;
          font-size: 14px;
        }
        
        .hero-section h1 {
          font-size: 36px;
        }
        
        .hero-section h2 {
          font-size: 18px;
        }
        
        .features-grid {
          grid-template-columns: 1fr;
        }
        
        .community-section {
          padding: 40px 20px;
        }
      }

      @media (max-width: 480px) {
        header {
          padding: 15px;
          flex-direction: column;
          gap: 15px;
        }
        
        .hero-section h1 {
          font-size: 28px;
        }
        
        .hero-section h2 {
          font-size: 16px;
        }
      }

      /* Dark mode */
      .dark-mode {
        --background-color: #1a1a2e;
        --card-color: #252541;
        --text-color: #e6e6f2;
        --light-text: #9999b8;
        --shadow: 0 8px 30px rgba(0, 0, 0, 0.2);
      }
    </style>
  </head>
  <body>
    <header>
      <div class="header-content">
        <a href="/" class="logo">
          <div class="logo-icon">
            <i class="fas fa-rocket"></i>
          </div>
          CareerPath
        </a>
        <div class="profile-pic-container">
          <img
            src="/uploads/<%= profilePic %>"
            alt="Profile Picture"
            class="profile-pic"
          />
          <span class="notification-dot"></span>
          <div class="dropdown-content">
            <a href="/home/<%= userId %>"><i class="fas fa-home"></i> Home</a>
            <a href="/dashboard/<%= userId %>"><i class="fas fa-chart-line"></i> Dashboard</a>
            <a href="/edit-profile/<%= userId %>"><i class="fas fa-user-edit"></i> Edit Profile</a>
            <a href="/notifications/<%= userId %>"><i class="fas fa-bell"></i> Notifications</a>
            <a href="/settings/<%= userId %>"><i class="fas fa-cog"></i> Settings</a>
            <a href="/logout"><i class="fas fa-sign-out-alt"></i> Logout</a>
          </div>
        </div>
        <div class="welcome-text">
          <h1>Welcome, <%= username %>!</h1>
          <h2>Your career journey starts here</h2>
        </div>
      </div>
      <div class="mode-toggle">
        <button class="mode-button active" id="explore-btn">Explore</button>
        <button class="mode-button" id="guide-btn">Guide</button>
      </div>
    </header>

    <main>
      <div class="hero-section">
        <h1>DISCOVER YOUR CAREER PATH</h1>
        <h2>Unlock your potential with personalized guidance, resources, and community support</h2>
      </div>

      <div class="features-grid" id="features-container">
        <div class="feature-card">
          <div class="feature-icon">
            <i class="fas fa-compass"></i>
          </div>
          <h3>EXPLORE CAREERS</h3>
          <p>Discover career paths perfectly aligned with your unique skills, interests, and aspirations</p>
          <a href="/recommendations/<%= userId %>" class="button">Explore Now</a>
        </div>

        <% if (hasSelectedJobs) { %>
        <div class="feature-card">
          <div class="feature-icon">
            <i class="fas fa-star"></i>
          </div>
          <h3>YOUR INTERESTS</h3>
          <p>Review and manage your selected career paths, stay focused on your professional goals</p>
          <a href="/your-interests/<%= userId %>" class="button">View Interests</a>
        </div>

        <div class="feature-card">
          <div class="feature-icon">
            <i class="fas fa-chart-bar"></i>
          </div>
          <h3>TRACK PROGRESS</h3>
          <p>Monitor your development journey and celebrate milestone achievements along the way</p>
          <a href="/progress/<%= userId %>" class="button">Check Progress</a>
        </div>

        <div class="feature-card">
          <div class="feature-icon">
            <i class="fas fa-comments"></i>
          </div>
          <h3>INTERVIEW PREP</h3>
          <p>Master the art of interviewing with tailored practice sessions, expert tips, and feedback</p>
          <a href="/interview-cracker/<%= userId %>" class="button">Practice Now</a>
        </div>

        <% } else { %>
        <div class="feature-card">
          <div class="feature-icon">
            <i class="fas fa-tasks"></i>
          </div>
          <h3>CAREER ASSESSMENT</h3>
          <p>Discover your strengths, preferences, and ideal career paths through our personalized assessment</p>
          <a href="/career-assessment/<%= userId %>" class="button">Start Assessment</a>
        </div>
        <% } %>

        <div class="feature-card">
          <div class="feature-icon">
            <i class="fas fa-book"></i>
          </div>
          <h3>LEARNING RESOURCES</h3>
          <p>Access curated courses, tutorials, and certifications to develop in-demand skills</p>
          <a href="/improve-skills/<%= userId %>" class="button">Start Learning</a>
        </div>

        <div class="feature-card">
          <div class="feature-icon">
            <i class="fas fa-laptop-code"></i>
          </div>
          <h3>PORTFOLIO PROJECTS</h3>
          <p>Build impressive project portfolios that showcase your skills to potential employers</p>
          <a href="/projects/<%= userId %>" class="button">View Projects</a>
        </div>
      </div>

      <div class="community-section">
        <h2>JOIN OUR COMMUNITY</h2>
        <p>Connect with peers, industry experts, and mentors. Ask questions, share experiences, and grow together on your career journey!</p>
        <a href="/community/<%= userId %>" class="button">Join the Community</a>
      </div>

      <% if (hasSelectedJobs) { %>
      <div class="resources-section">
        <h2 class="section-title">RECOMMENDED FOR YOU</h2>
        
        <h3>Professional Certifications</h3>
        <div id="certificationsList">
          <!-- Will be populated by JavaScript -->
          <div class="resource-item">
            <h4>Loading certifications...</h4>
            <div class="progress-container">
              <div class="progress-bar"></div>
            </div>
          </div>
        </div>
        
        <h3>Free Learning Resources</h3>
        <div id="resourcesList">
          <!-- Will be populated by JavaScript -->
          <div class="resource-item">
            <h4>Loading resources...</h4>
            <div class="progress-container">
              <div class="progress-bar"></div>
            </div>
          </div>
        </div>
      </div>

      <div style="margin-top: 50px;">
        <h2 class="section-title">UPCOMING EVENTS</h2>
        <p class="text-center">Enhance your skills and network with industry professionals at our upcoming events and workshops!</p>
        <div class="text-center" style="margin-top: 30px;">
          <a href="/events/<%= userId %>" class="button outline">View All Events</a>
        </div>
      </div>
      <% } %>
    </main>

    <footer>
      <div class="footer-content">
        <div class="footer-column">
          <h3>About Us</h3>
          <ul class="footer-links">
            <li><a href="/about"><i class="fas fa-angle-right"></i> Our Mission</a></li>
            <li><a href="/team"><i class="fas fa-angle-right"></i> Our Team</a></li>
            <li><a href="/careers"><i class="fas fa-angle-right"></i> Careers</a></li>
            <li><a href="/press"><i class="fas fa-angle-right"></i> Press</a></li>
          </ul>
        </div>
        
        <div class="footer-column">
          <h3>Resources</h3>
          <ul class="footer-links">
            <li><a href="/blog"><i class="fas fa-angle-right"></i> Blog</a></li>
            <li><a href="/guides"><i class="fas fa-angle-right"></i> Career Guides</a></li>
            <li><a href="/templates"><i class="fas fa-angle-right"></i> Resume Templates</a></li>
            <li><a href="/faq"><i class="fas fa-angle-right"></i> FAQ</a></li>
          </ul>
        </div>
        
        <div class="footer-column">
          <h3>Support</h3>
          <ul class="footer-links">
            <li><a href="/contact"><i class="fas fa-angle-right"></i> Contact Us</a></li>
            <li><a href="/help"><i class="fas fa-angle-right"></i> Help Center</a></li>
            <li><a href="/feedback"><i class="fas fa-angle-right"></i> Feedback</a></li>
            <li><a href="/report-issue"><i class="fas fa-angle-right"></i> Report an Issue</a></li>
          </ul>
        </div>
        
        <div class="footer-column">
          <h3>Connect With Us</h3>
          <p>Stay updated with the latest career opportunities and resources</p>
          <div class="social-links">
            <a href="#"><i class="fab fa-facebook-f"></i></a>
            <a href="#"><i class="fab fa-twitter"></i></a>
            <a href="#"><i class="fab fa-linkedin-in"></i></a>
            <a href="#"><i class="fab fa-instagram"></i></a>
          </div>
        </div>
      </div>
      
      <div class="footer-bottom">
        <p>&copy; 2025 Your Company Name. All Rights Reserved.</p>
      </div>
</footer>
