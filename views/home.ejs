<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home</title>
    <style>
        .community-button {
            background-color: #4CAF50; /* Green */
            color: white;
            padding: 10px 20px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            margin-top: 20px;
        }
        .community-button:hover {
            background-color: #45a049; /* Darker green on hover */
        }
    </style>
</head>
<body>
    <header>
        <img src="/uploads/<%= profilePic %>" alt="Profile Picture" style="width: 50px; height: 50px; border-radius: 50%;" onclick="window.location.href='/dashboard/<%= userId %>'">
        <h1>Welcome, <%= username %>!</h1>
    </header>

    <main>
        <% if (hasSelectedJobs) { %>
            <button onclick="window.location.href='/your-interests/<%= userId %>'">Your Interest</button>
            <button onclick="window.location.href='/progress/<%= userId %>'">Your Progress</button>
        <% } else { %>
            <button onclick="window.location.href='/recommendations/<%= userId %>'">Recommended for You</button>
        <% } %>

        <!-- Community Section -->
        <div style="margin-top: 30px;">
            <h2>Community</h2>
            <p>Connect with others, ask questions, and share your career journey!</p>
            <button class="community-button" onclick="window.location.href='/community/<%= userId %>'">Join the Community</button>
        </div>
    </main>
</body>
</html>