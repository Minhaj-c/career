<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Recommended Projects</title>
    <style>
        .project-card {
            border: 1px solid #ccc;
            border-radius: 8px;
            padding: 16px;
            margin: 16px 0;
            background-color: #f9f9f9;
        }
        .project-title {
            font-size: 1.5em;
            margin-bottom: 8px;
        }
        .project-description {
            margin-bottom: 8px;
        }
        .project-technologies {
            font-style: italic;
            color: #555;
        }
        .project-difficulty, .project-duration {
            font-weight: bold;
        }
    </style>
</head>
<body>
    <header>
        <h1>Recommended Projects for <%= selectedJob %></h1>
        <p>Welcome, <%= username %>! Here are some project ideas tailored for your selected job.</p>
    </header>

    <main>
        <% if (recommendedProjects.length > 0) { %>
            <% recommendedProjects.forEach(project => { %>
                <div class="project-card">
                    <div class="project-title"><%= project.title %></div>
                    <div class="project-description"><%= project.description %></div>
                    <div class="project-technologies">Technologies: <%= project.technologies.join(", ") %></div>
                    <div class="project-difficulty">Difficulty: <%= project.difficulty %></div>
                    <div class="project-duration">Duration: <%= project.duration %></div>
                </div>
            <% }) %>
        <% } else { %>
            <p>No project recommendations available for your selected job.</p>
        <% } %>
    </main>

    <footer>
        <button onclick="window.location.href='/home/<%= userId %>'">Go Back to Home</button>
    </footer>
</body>
</html>