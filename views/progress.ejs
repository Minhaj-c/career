
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Your Progress | CareerPath</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" />
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/animate.css/4.1.1/animate.min.css" />
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap');
        
        :root {
            --primary-color: #2a3990;
            --secondary-color: #4e54c8;
            --accent-color: #ff6b6b;
            --background-color: #f8f9ff;
            --card-color: #ffffff;
            --text-color: #333344;
            --light-text: #7b7b8f;
            --shadow: 0 8px 30px rgba(0, 0, 0, 0.08);
            --hover-shadow: 0 15px 35px rgba(0, 0, 0, 0.12);
            --transition: all 0.3s cubic-bezier(0.25, 0.8, 0.25, 1);
        }

        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
        }

        body {
            font-family: 'Poppins', sans-serif;
            margin: 0;
            padding: 0;
            background-color: var(--background-color);
            color: var(--text-color);
            overflow-x: hidden;
        }

        /* Header styles */
        header {
            background-color: var(--card-color);
            padding: 15px 30px;
            display: flex;
            flex-direction: column;
            position: relative;
            box-shadow: var(--shadow);
            z-index: 1000;
            transition: var(--transition);
            text-align: center;
            margin-bottom: 40px;
        }

        header:hover {
            box-shadow: var(--hover-shadow);
        }

        header h1 {
            font-size: 28px;
            background: linear-gradient(135deg, var(--primary-color), var(--secondary-color));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            margin-bottom: 10px;
            position: relative;
            display: inline-block;
            animation: fadeInUp 1s ease-out;
        }

        header p {
            color: var(--light-text);
            margin-bottom: 20px;
            animation: fadeInUp 1.2s ease-out;
        }

        .go-home-btn {
            display: inline-block;
            padding: 12px 28px;
            background: linear-gradient(135deg, var(--primary-color), var(--secondary-color));
            color: white;
            border: none;
            border-radius: 30px;
            cursor: pointer;
            text-decoration: none;
            font-weight: 500;
            transition: var(--transition);
            position: relative;
            overflow: hidden;
            z-index: 1;
            box-shadow: 0 4px 15px rgba(42, 57, 144, 0.2);
            font-family: 'Poppins', sans-serif;
            margin: 0 auto;
            animation: fadeInUp 1.4s ease-out;
        }

        .go-home-btn::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.2), transparent);
            transition: all 0.6s;
            z-index: -1;
        }

        .go-home-btn:hover {
            transform: translateY(-3px);
            box-shadow: 0 7px 20px rgba(42, 57, 144, 0.3);
        }

        .go-home-btn:hover::before {
            left: 100%;
        }

        /* Main content styles */
        main {
            max-width: 1000px;
            margin: 0 auto;
            padding: 0 20px 40px;
        }

        /* Progress container styles */
        .progress-container {
            background-color: var(--card-color);
            border-radius: 20px;
            overflow: hidden;
            box-shadow: var(--shadow);
            padding: 30px;
            transition: var(--transition);
            position: relative;
            z-index: 1;
            margin-bottom: 40px;
            animation: fadeIn 1s ease-out;
        }

        .progress-container:hover {
            transform: translateY(-5px);
            box-shadow: var(--hover-shadow);
        }

        .progress-container h2 {
            color: var(--primary-color);
            margin-bottom: 20px;
            font-size: 22px;
            position: relative;
            padding-bottom: 10px;
        }

        .progress-container h2::after {
            content: '';
            position: absolute;
            bottom: 0;
            left: 0;
            width: 60px;
            height: 3px;
            background: linear-gradient(135deg, var(--primary-color), var(--secondary-color));
            border-radius: 2px;
        }

        .progress-container p {
            color: var(--light-text);
            margin-bottom: 20px;
            font-size: 16px;
        }

        .progress-bar {
            width: 100%;
            background-color: #e0e0e0;
            border-radius: 10px;
            overflow: hidden;
            margin: 20px 0;
            height: 15px;
        }

        .progress-bar-fill {
            height: 100%;
            background: linear-gradient(90deg, var(--primary-color), var(--secondary-color));
            border-radius: 10px;
            transition: width 1.5s ease-in-out;
        }

        /* Weeks container styles */
        .weeks-container {
            margin-top: 40px;
            animation: fadeIn 1.5s ease-out;
        }

        .weeks-container h2 {
            color: var(--primary-color);
            margin-bottom: 30px;
            font-size: 22px;
            position: relative;
            padding-bottom: 10px;
            text-align: center;
        }

        .weeks-container h2::after {
            content: '';
            position: absolute;
            bottom: 0;
            left: 50%;
            transform: translateX(-50%);
            width: 80px;
            height: 3px;
            background: linear-gradient(135deg, var(--primary-color), var(--secondary-color));
            border-radius: 2px;
        }

        .week-container {
            background-color: var(--card-color);
            border-radius: 15px;
            overflow: hidden;
            box-shadow: var(--shadow);
            padding: 25px;
            transition: var(--transition);
            position: relative;
            z-index: 1;
            margin-bottom: 20px;
            border-left: 5px solid #e0e0e0;
        }

        .week-container::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 0;
            background: linear-gradient(135deg, rgba(42, 57, 144, 0.03), rgba(78, 84, 200, 0.05));
            transition: var(--transition);
            z-index: -1;
        }

        .week-container:hover {
            transform: translateY(-5px);
            box-shadow: var(--hover-shadow);
        }

        .week-container:hover::before {
            height: 100%;
        }

        .week-container h3 {
            color: var(--primary-color);
            margin-bottom: 10px;
            font-size: 18px;
        }

        .week-container p {
            color: var(--light-text);
            margin-bottom: 15px;
        }

        .week-completed {
            border-left: 5px solid #4CAF50;
            background-color: rgba(76, 175, 80, 0.05);
        }

        .week-completed h3 {
            display: flex;
            align-items: center;
        }

        .week-completed h3::before {
            content: '\f058';
            font-family: 'Font Awesome 5 Free';
            font-weight: 900;
            color: #4CAF50;
            margin-right: 10px;
            font-size: 20px;
        }

        .status-badge {
            display: inline-block;
            padding: 5px 12px;
            border-radius: 20px;
            font-size: 14px;
            font-weight: 500;
        }

        .badge-completed {
            background-color: rgba(76, 175, 80, 0.15);
            color: #2E7D32;
        }

        .badge-pending {
            background-color: rgba(78, 84, 200, 0.15);
            color: var(--primary-color);
        }

        /* Animations */
        @keyframes fadeIn {
            from {
                opacity: 0;
            }
            to {
                opacity: 1;
            }
        }

        @keyframes fadeInUp {
            from {
                opacity: 0;
                transform: translateY(20px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        /* Responsive design */
        @media (max-width: 768px) {
            header {
                padding: 15px 20px;
            }
            
            header h1 {
                font-size: 24px;
            }
            
            .progress-container, .week-container {
                padding: 20px;
            }
        }

        @media (max-width: 480px) {
            header h1 {
                font-size: 20px;
            }
            
            .go-home-btn {
                padding: 10px 20px;
                font-size: 14px;
            }
            
            .progress-container h2, .weeks-container h2 {
                font-size: 18px;
            }
            
            .week-container h3 {
                font-size: 16px;
            }
        }
    </style>
</head>
<body>
    <header>
        <h1>Your Progress - <%= selectedJob %></h1>
        <p>Welcome <%= username %>! Here's your progress so far.</p>
        <button onclick="window.location.href='/home/<%= userId %>'" class="go-home-btn">
            <i class="fas fa-home"></i> Go Back to Home
        </button>
    </header>

    <main>
        <div class="progress-container">
            <h2>Progress Overview</h2>
            <p>You have completed <strong><%= completedWeeks %></strong> out of <strong><%= totalWeeks %></strong> weeks.</p>
            <div class="progress-bar">
                <div class="progress-bar-fill" style="width: <%= progressPercentage %>%;"></div>
            </div>
            <p style="text-align: right; margin-top: 5px;"><strong><%= progressPercentage %>%</strong> completed</p>
        </div>

        <div class="weeks-container">
            <h2>Weekly Progress</h2>
            <% weeks.forEach((week, index) => { %>
                <div class="week-container <%= progress.completedWeeks.includes(index + 1) ? 'week-completed' : '' %> animate-card" style="animation-delay: <%= 0.1 * index %>s">
                    <h3><%= week.title %></h3>
                    <p><%= week.description %></p>
                    <% if (progress.completedWeeks.includes(index + 1)) { %>
                        <div class="status-badge badge-completed">
                            <i class="fas fa-check-circle"></i> Completed
                        </div>
                    <% } else { %>
                        <div class="status-badge badge-pending">
                            <i class="fas fa-clock"></i> Not Completed
                        </div>
                    <% } %>
                </div>
            <% }) %>
        </div>
    </main>

    <script>
        // Animation for progress bar
        document.addEventListener('DOMContentLoaded', function() {
            setTimeout(function() {
                document.querySelector('.progress-bar-fill').style.width = '<%= progressPercentage %>%';
            }, 300);
            
            // Animate cards on scroll
            const animateCards = document.querySelectorAll('.animate-card');
            
            function checkIfInView() {
                animateCards.forEach(function(card, index) {
                    const rect = card.getBoundingClientRect();
                    const isInView = (
                        rect.top >= 0 &&
                        rect.bottom <= (window.innerHeight || document.documentElement.clientHeight)
                    );
                    
                    if (isInView) {
                        setTimeout(function() {
                            card.style.opacity = '1';
                            card.style.transform = 'translateY(0)';
                        }, 100 * index);
                    }
                });
            }
            
            // Set initial state for animations
            animateCards.forEach(function(card) {
                card.style.opacity = '0';
                card.style.transform = 'translateY(20px)';
                card.style.transition = 'all 0.5s ease-out';
            });
            
            // Check on load and scroll
            window.addEventListener('scroll', checkIfInView);
            window.addEventListener('resize', checkIfInView);
            checkIfInView();
        });
    </script>
</body>
</html>