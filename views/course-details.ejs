<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Course Details</title>
    <style>
        .resource-links {
            margin-top: 20px;
        }
        .resource-links a {
            display: inline-block;
            margin-right: 15px;
            margin-bottom: 10px;
            padding: 8px 15px;
            background-color: #f0f0f0;
            border-radius: 4px;
            text-decoration: none;
            color: #333;
            transition: background-color 0.3s;
        }
        .resource-links a:hover {
            background-color: #e0e0e0;
        }
    </style>
</head>
<body>
    <header>
        <h1>Course Details for <%= course %></h1>
    </header>
    
    <main>
        <% if (courseDetails) { %>
            <h3>Course Description</h3>
            <p><%= courseDetails.description %></p>
            
            <% if (courseDetails.externalLinks) { %>
                <h3>External Resources</h3>
                <div class="resource-links">
                    <% Object.entries(courseDetails.externalLinks).forEach(([platform, link]) => { %>
                        <a href="<%= link %>" target="_blank"><%= platform.charAt(0).toUpperCase() + platform.slice(1) %></a>
                    <% }) %>
                </div>
            <% } %>
        <% } else { %>
            <p>No details found for this course.</p>
        <% } %>
        
        <!-- Updated Back Button -->
        <% if (previousJob) { %>
            <button onclick="window.location.href='/job-details/<%= previousJob %>'">Back to Job Details</button>
        <% } else { %>
            <button onclick="window.location.href='/recommendations'">Back to Recommendations</button>
        <% } %>
    </main>
</body>
</html>